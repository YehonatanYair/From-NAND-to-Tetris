/**
 * The main class of the TREX game.
 */
class Main {
    function void main() {
		/** Initializes a TREX game and starts running it. */
        var TREX game;
		let game = TREX.new();
		do game.run();
        do game.dispose();
        return;
    }
}
